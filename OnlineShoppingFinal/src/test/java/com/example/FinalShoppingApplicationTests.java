package com.example;

import org.junit.Assert;
import org.junit.Test;

import org.springframework.beans.factory.annotation.Autowired;
import org.springframework.boot.test.context.SpringBootTest;
import com.project.model.Category;
import com.project.repository.CategoryRepository;
@SpringBootTest
public class FinalShoppingApplicationTests {

	@Test
	public void contextLoads() {
	}

	 //@Autowired
		/*CategoryRepository categoryRepository;
	    public void testFindById()
	  
	    {
	    	  Category assignedcategoryt = categoryRepository.findOne((long) 01);

	        Assert.assertEquals("TV", assignedcategoryt.getName());
	        Assert.assertEquals("LG",assignedcategoryt.getDescription());
	       
	        return;
	    }
*/

}
